name: Sync next-demo-internal → next-demo-example

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'next-demo-internal/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to koryla-next-demo-example
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          cp -r next-demo-internal /tmp/next-demo-target

          cd /tmp/next-demo-target
          git init
          git checkout -b main
          git remote add origin https://andresclua:${GH_PAT}@github.com/andresclua/koryla-next-demo-example.git
          git add -A
          git commit -m "sync: $(date -u +'%Y-%m-%d %H:%M:%S')"
          git push origin main --force
